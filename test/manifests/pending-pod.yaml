apiVersion: v1
kind: Pod
metadata:
  name: pending-pod
spec:
  containers:
  - name: giant-eater
    image: nginx
    resources:
      requests:
        # 申请 100个 CPU 和 100G 内存，导致 Pod 一直处于 Pending 状态
        cpu: "100"
        memory: "100Gi"